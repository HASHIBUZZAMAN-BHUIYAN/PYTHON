# Day 09 — Exercises

# Exercise 1 — Line counter
# Write count_lines(filepath) that returns the number of non-empty lines
# in a text file.
# Test: create a temp file with 5 lines (2 blank), verify count is 3.

# Exercise 2 — Word frequency from file
# Write word_freq(filepath) that reads a text file and returns a dict
# {word: count}, case-insensitive, stripping punctuation.

# Exercise 3 — CSV filter
# Create a CSV file "scores.csv" with columns Name,Subject,Score.
# Add at least 6 rows. Then read it back and print only rows where Score > 80.

# Exercise 4 — CSV stats
# Read "scores.csv" (from exercise 3). Compute average score per subject.
# Print a summary table.

# Exercise 5 — Todo file
# Implement a simple file-backed todo list:
# - save_todos(todos, filepath): writes list of strings, one per line
# - load_todos(filepath): reads lines and returns a list
# - Test: add 3 items, save, load, print.
