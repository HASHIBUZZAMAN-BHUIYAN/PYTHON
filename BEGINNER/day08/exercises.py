# Day 08 — Exercises

# Exercise 1 — is_prime(n)
# Write a function is_prime(n) that returns True if n is prime, False otherwise.
# Test with: 2, 3, 4, 17, 25, 97

# Exercise 2 — Flexible calculator
# Write calc(*numbers, op="sum") that accepts any number of values and
# an operation keyword: "sum", "product", "mean", "max", "min".

# Exercise 3 — Flatten nested list (recursive)
# Write flatten(lst) that recursively flattens any depth of nesting.
# flatten([1, [2, [3, 4], 5], 6]) → [1, 2, 3, 4, 5, 6]

# Exercise 4 — Memoised Fibonacci
# The naive recursive Fibonacci is slow for large n.
# Re-implement it using a dict to cache already-computed results.
# memo = {}
# def fib(n, memo): ...
# Print fib(40) — should return instantly.

# Exercise 5 — Decorator intro (high-order function)
# Write a function timer(func) that wraps func, measures how long it takes,
# prints the time, then returns the result.
# Test it by wrapping a function that sleeps 0.1 seconds.
# (import time; time.sleep(0.1))
