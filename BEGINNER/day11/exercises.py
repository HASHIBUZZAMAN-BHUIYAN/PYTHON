# Day 11 — Exercises

# Exercise 1 — Rectangle class
# Attributes: width, height
# Methods: area(), perimeter(), is_square() → bool, __str__

# Exercise 2 — Student class
# Attributes: name, grades (list, starts empty)
# Methods: add_grade(score), average(), highest(), lowest(), __str__

# Exercise 3 — Counter class
# Attributes: count (starts at 0), step (default 1)
# Methods: increment(), decrement(), reset(), __str__
# Class attribute: total_counters (tracks how many Counter objects exist)

# Exercise 4 — Temperature class
# Store temperature internally in Celsius.
# Properties (or methods): to_fahrenheit(), to_kelvin()
# Class method from_fahrenheit(f) that creates a Temperature from °F.
# __str__ shows "X °C"

# Exercise 5 — Stack class
# Implement a stack using a list internally.
# Methods: push(item), pop() (raises IndexError if empty),
#          peek() (see top without removing), is_empty(), __len__, __str__
